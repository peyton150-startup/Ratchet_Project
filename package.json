{
  "name": "ratchet",
  "version": "0.0.1",
  "private": true,
  "description": "Ratchet \u2014 multi-tenant task orchestration platform",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "test": "pnpm -r test",
    "migrate": "pnpm --filter @workspace/api migrate",
    "load": "k6 run load/ingest.js"
  },
  "license": "ISC"
}
