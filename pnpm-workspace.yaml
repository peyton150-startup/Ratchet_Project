packages:
  - 'packages/api'
  - 'packages/workers'
  - 'packages/web'
  - 'packages/sdk'

# Allow esbuild (pulled in by tsx) to run its install script so its native
# binary is fetched during `pnpm install` in CI.
onlyBuiltDependencies:
  - esbuild
