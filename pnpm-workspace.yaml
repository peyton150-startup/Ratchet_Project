packages:
  - 'packages/api'
  - 'packages/workers'
  - 'packages/web'
  - 'packages/sdk'

# esbuild (pulled in by tsx and vite) must run its install script to fetch its native binary.
allowBuilds:
  esbuild: true

onlyBuiltDependencies:
  - esbuild
